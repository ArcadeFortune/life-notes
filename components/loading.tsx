"use client";
import { RefreshCw } from "lucide-react";

export default function Loading() {
  return <RefreshCw className="animate-[spin_1s_ease-in-out_infinite,pulse_1s_ease-in-out_infinite] " />;
}
